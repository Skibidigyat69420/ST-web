# Cloudflare Pages configuration
# Rename the KV namespace binding below to match the one created in your Cloudflare dashboard.
# Dashboard path: Workers & Pages → Your Pages project → Settings → Functions → KV namespace bindings

[[kv_namespaces]]
binding = "CONTACT_SUBMISSIONS"
id = "<YOUR_KV_NAMESPACE_ID>"
